// console.log(sum)c
function rangeF(start, end, inc) {
    // start = start || 0;
    z = "";
    for (start; start <= end; start = start + 1) {
        // if (x <= 10) {
        z = +start + +z;
        // console.log(increment);
        // console.log(z);
        // }
        // var sum = +z + x - 10;
    }
}
console.log(rangeF(typeof increment))
console.log(rangeF(1, 10))