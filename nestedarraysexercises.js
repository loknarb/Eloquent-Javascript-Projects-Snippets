function rotate(arr, num) {
    for (let i = 0; i < arr.length; i++) {
        // * needs a +1 or -1 somewhere here
        if (arr.length == num) {
        }
    }
}

rotate([1, 2, 3], 1); // [3,1,2]
rotate([1, 2, 3], 2); // [2,3,1]
rotate([1, 2, 3], 3); // [1,2,3]
